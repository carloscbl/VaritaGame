using UnityEngine;
using System.Collections;

public class sp : MonoBehaviour {

	// Use this for initialization
	void Start () {
        this.GetComponent<UnityEngine.UI.Text>().text = "10";
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
